Source: https://www.hackerrank.com/challenges/insert-a-node-into-a-sorted-doubly-linked-list/problem

Problem Statement
Given a reference to the head of a doubly-linked list and 
an integer, data, create a new DoublyLinkedListNode object 
having data value data and insert it into a sorted linked 
list while maintaining the sort.

Code:

#function

def sortedInsert(head, data):
    nnode = DoublyLinkedListNode(data)
    if head == None:
        return nnode
    elif data<head.data:
        nnode.next = head
        head.prev = nnode
        return nnode
    else:
        nnode = sortedInsert(head.next, data)
        head.next  nnode
